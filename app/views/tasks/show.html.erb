<div class="container">
  <div>
    <h1><%= @task.title %></h1>
  </div>
  <div>
    <%= @task.completed ? '✔' : '✖' %>
    <%= @task.details %>
    <%= link_to '🗑️', delete_task_path(@task), method: :delete, data: { confirm: 'Are you sure?' } %>
  </div>
  <div>
    <%= link_to 'Go back', tasks_path, class:'link' %>
    <%= link_to 'Edit', edit_task_path, class:'link' %>
  </div>
</div>